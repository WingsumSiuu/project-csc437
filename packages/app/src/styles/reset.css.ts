import { css } from "lit";

const styles = css`
    img {
        max-width: 100%;
    }

    body {
        margin: 0;
        background-color: var(--color-page-bg);

    }
    
    .body-content {
        margin: 0 3em 0 3em;

    }
    
`;

export default { styles };
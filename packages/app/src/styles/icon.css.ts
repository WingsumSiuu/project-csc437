import { css } from "lit";

const styles = css`
    svg.icon {
        height: 2em;
        width: 2em;
        display: inline;
        vertical-align: text-top;
        fill: var(--color-text);
    }`;

export default { styles };